/*============================================================================
  Brooklyn by Shopify
  Custom stylings for Cuppé shop
==============================================================================*/
/*============================================================================
  Dependency-free breakpoint mixin
    - http://blog.grayghostvisuals.com/sass/sass-media-query-mixin/
==============================================================================*/
$min: min-width;
$max: max-width;
@mixin at-query ($constraint, $viewport1, $viewport2:null) {
  @if $constraint == $min {
    @media screen and ($min: $viewport1) {
      @content;
    }
  } @else if $constraint == $max {
    @media screen and ($max: $viewport1) {
      @content;
    }
  } @else {
    @media screen and ($min: $viewport1) and ($max: $viewport2) {
      @content;
    }
  }
}

/*============================================================================
  Flexbox prefix mixins from Bourbon
    https://github.com/thoughtbot/bourbon/blob/master/app/assets/stylesheets/css3/_flex-box.scss
==============================================================================*/
$viewportIncrement: 1px;
$siteWidth: 1050px;
$small: 590px;
$medium: 768px;
$large: 769px;
$xlarge: $siteWidth + $viewportIncrement;

$postSmall: $small + $viewportIncrement;
$preMedium: $medium - $viewportIncrement;
$preLarge: $large - $viewportIncrement;

/*================ Cuppe-specific styles ================*/
/* for Decorative-pattern.liquid section starts */
.cuppe-pattern {
  &.svg-inherit-color { 
    svg { fill: currentColor; }
  }
  &.show--pattern-mobile {
    @include at-query($min, $small) { display: none; }
  }
  &.show--pattern-desktop {
    @include at-query($max, $small) { display: none; }
  }
}
/* Decorative-pattern.liquid section ends */

/* UTILITY CLASS */
.page-width-100 {
  width: 100%;
}
.section-header__title {
  padding: 0 30px;
}
.site-header .burger-icon {
  background-color: black !important;
}
#AccessibleNav .site-nav__link,
.site-nav--mobile .site-nav__link {
  color: black !important;
  font-family: inherit;
  
}
#AccessibleNav .site-nav__link:hover, 
.site-nav--mobile .site-nav__link:hover {
  background-color: rgba(255,255,255,0.75);
}
.section-header { padding-top: 50px; }

// Custom style for header title of Rich Text section
.section-header__title { text-align: left; }
// Custom style for header title of Rich Text section to override the store-wide text-align: left 
.rich-text-section .section-header__title { text-align: center; }

/* accordion for expandable product description & FAQs */
/*-------- Accordion starts -------*/
@import url("https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css");
.cuppe-acc-kontainer {
  width: 100%;
  margin: auto;
}
.cuppe-acc-kontainer .acc-body {
  width: 98%;
  width: calc(100% - 20px);
  margin: 0 auto;
  height: 0;
  opacity: 0;
  color: rgba(0, 0, 0, 0);
  background-color: rgba(255, 255, 255, 0.2);
  line-height: 28px;
  padding: 0 20px;
  box-sizing: border-box;
  transition: 0.5s;
}

.cuppe-acc-kontainer label {
  cursor: pointer;
  background-color: rgba(255, 255, 255, 0.1);
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  display: block;
  padding: 15px;
  width: 100%;
  color: #404040;
  font-weight: bold !important;
  box-sizing: border-box;
  z-index: 100;
  font-style: normal;
}

.cuppe-acc-kontainer input{
  display: none;
}

.cuppe-acc-kontainer label:before {
  font-family: 'FontAwesome';
  content: '\f106';
  font-weight: bolder;
  float: right;
}

.cuppe-acc-kontainer input:checked+label {
  background-color: rgba(255, 255, 255, 0.15);
}

.cuppe-acc-kontainer input:checked+label:before {
  font-family: 'FontAwesome';
  content: '\f107';
  transition: 0.5s;
}

.cuppe-acc-kontainer input:checked~.acc-body {
  height: auto;
  color: #808080;
  font-size: 16px;
  padding: 20px;
  transition: 0.5s;
  opacity: 100;
}

/*--------New Accordion ends-------*/
.cuppe-details > .cuppe-summary {
  list-style: none;
}
.cuppe-font-accordion-label {
  color: #404040;
  font-weight: 700;
}
.cuppe-font-accordion-content {
  color: grey;
}
.cuppe-summary {
  cursor: pointer;
  padding: 0 1rem;
  width: 100%;
  display: flex;
  justify-content: space-between;
}
.cuppe-summary::after {
  font-family: "FontAwesome";
  font-weight: bolder;
  margin-left: 1rem;
}
.cuppe-summary > * {
  margin-bottom: 0px !important;
}
.cuppe-details[open] > .cuppe-details-content {
  opacity: 1;
  font-size: 1rem;
  padding: 1.25rem;
}
.cuppe-details:not([open]) > .cuppe-summary::after {
  content: "\f106";
}
.cuppe-details[open] > .cuppe-summary::after {
  content: "\f107";
}
.cuppe-details {
  width: 98%;
  margin: auto;
  padding-top: 1rem;
}
.cuppe-details-content {
  animation-duration: 500ms;
  animation-name: slidein;
  animation-direction: alternate;
  opacity: 0;
  line-height: 28px;
  transition: all 500ms;
  box-sizing: border-box;
  margin-left: 1rem;
  padding: 0 1rem;
  list-style: initial;
  position: relative;
}
.cuppe-details-content > :only-child {
  margin-bottom: 0rem !important;
}
.cuppe-details-content > :not(:only-child) {
  margin-bottom: 0.5rem !important;
}
@keyframes slidein {
  from {
    opacity: 0;
    font-size: unset;
    padding: 0 1rem;
  }

  to {
    opacity: 1;
    font-size: 1rem;
    padding: 1.25rem;
  }
}

/*--------Accordion ends-------*/

/*================ Page-specific styles ================*/
/* Our Story section on Homepage */
.our-story-section .featured-row__subtext {
  max-width: 600px;
}
/* Grid product items as used in Product Recommendation */
.grid-product__image-link {
  background-color: transparent;
}
.product-recommendations {
  margin-top: 0;
}
.product-single__thumbnails img {
  max-height: 80px !important;
}
.product-single__price, .product-single__price--compare-at {
  font-size: 1.5em;
}
.product-single__meta .product-single__title,
.product-single__meta div[itemprop="offers"],
.product-rating-stampedio {
  text-align: left;
}
.product-single__meta div[itemprop="offers"] hr {
  margin: 20px 0;
}
.product-single__meta .single-option-radio {
  padding-left: 0;
  padding-right: 0;
}
.product-single__meta .single-option-radio label {
  margin: 0;
}
